---
# https://taskfile.dev

version: "3"

includes:
  pkg:
    taskfile: ./pkg/Taskfile.yml
    dir: ./pkg

tasks:
  install:
    desc: Install dependencies for all subprojects.
    cmds:
      - task: pkg:install

  format:
    desc: Format the entire repository.
    cmds:
      - stylua --config-path .stylua.toml . -v
      - task: pkg:format

  lint:
    desc: Lint the entire repository (Lua + Python).
    cmds:
      - stylua --config-path .stylua.toml --check .
      - selene .
      - task: pkg:lint

  test-lua:
    desc: Run the Lua test suite via mini.test (headless nvim).
    cmds:
      - nvim --headless --clean -u scripts/minimal-init.lua -c "lua MiniTest.run()" -c "qa!"

  test:
    desc: Run tests across all subprojects.
    cmds:
      - task: test-lua
      - task: pkg:test
